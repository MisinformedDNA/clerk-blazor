{
  "Clerk": {
    // Replace with your Clerk Publishable Key (pk_test_… or pk_live_…).
    // You can find this in the Clerk Dashboard under API Keys.
    // This value is safe to expose in the browser bundle — it is NOT a secret.
    //
    // For local development, override this in wwwroot/appsettings.Development.json.
    //
    // For CI/CD, substitute the value at build time from the CLERK_PUBLISHABLE_KEY
    // GitHub Secret — see README.md for a sample GitHub Actions workflow.
    "PublishableKey": "YOUR_CLERK_PUBLISHABLE_KEY"
  },
  "Api": {
    "BaseUrl": ""
  }
}
